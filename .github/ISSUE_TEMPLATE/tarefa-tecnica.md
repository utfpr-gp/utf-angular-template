---
name: 🟡 Tarefa técnica
about: Trabalho que não muda o comportamento do produto para o usuário
labels: manutencao
---

<!-- Tarefa técnica NÃO passa pelo ciclo de spec: ninguém demonstra uma atualização
     de dependência para quem não programa. Exemplos: subir a versão do Angular,
     extrair um componente repetido, ajustar lint, configurar a esteira de CI,
     trocar o json-server pelo BaaS numa coleção já existente. -->

## O que precisa ser feito

## Por que agora

<!-- Se esta tarefa nasceu de um `Assume que` de alguma spec, cite a Issue e o
     `// TODO #<n>` que ficou no código — é essa dívida que ela está pagando. -->

## Como saber que ficou pronto

<!-- Mesmo sem spec, precisa haver um critério observável: "a suíte continua verde e
     o build passa na versão nova", "a tela renderiza igual antes e depois". -->

---

<!-- PR para a `develop`, com a etiqueta `manutencao` e sem `spec.md` — mas COM a
     explicação de 250 caracteres: a etiqueta dispensa a spec, nunca o Portão de
     Entendimento. -->
